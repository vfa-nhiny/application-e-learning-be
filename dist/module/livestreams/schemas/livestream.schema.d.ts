import * as mongoose from "mongoose";
export declare const LivestreamSchema: mongoose.Schema<any, mongoose.Model<any, any, any, any, any>, {}, {}, {}, {}, "type", {
    userId: string;
    userName: string;
}>;
